#pragma once
enum class Buttons
{
	Up = 0, // per something I read, I think on StackOverflow, the expected default numbering isn't guaranteed in an "enum class"
	Down = 1,
	Left = 2,
	Right = 3,
	A = 4,
	B = 5,
	X = 6,
	Y = 7,
	Stick = 8
};

